<%= form_for(scientific_work) do |f| %>
  <% if scientific_work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scientific_work.errors.count, "error") %> prohibited this scientific_work from being saved:</h2>

      <ul>
      <% scientific_work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :notes %>
    <%= f.text_area :notes %>
  </div>

  <div class="field">
    <%= f.label :profile_id %>
    <%= f.text_field :profile_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
